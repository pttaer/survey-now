using UnityEngine;

public class PrefsUtils
{
    public static PrefsUtils Api;

    /*public void SetCacheEmail(string cacheEmail)
    {
        PlayerPrefs.SetString(FAMConstant.EMAIL_CACHE, cacheEmail);
    }

    public string GetCacheEmail()
    {
        return PlayerPrefs.GetString(FAMConstant.EMAIL_CACHE);
    }

    public void SetCachePassword(string cachePassword)
    {
        PlayerPrefs.SetString(FAMConstant.PASSWORD_CACHE, cachePassword);
    }

    public string GetCachePassword()
    {
        return PlayerPrefs.GetString(FAMConstant.PASSWORD_CACHE);
    }

    public void ClearCacheLoginRemember()
    {
        PlayerPrefs.DeleteKey(FAMConstant.EMAIL_CACHE);
        PlayerPrefs.DeleteKey(FAMConstant.PASSWORD_CACHE);
    }*/
}